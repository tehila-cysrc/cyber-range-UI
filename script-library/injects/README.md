# Inject Library

Each YAML file is one inject: **Trigger → Script → Target role → Student observations**.

Triggers supported in this catalog (product wiring later):

- `manual` — Instructor fires from console
- `elapsed_seconds` — T+N after team scenario `started_at`

Actions today always mean: run the referenced script via existing Azure Run Command / Script Library.
